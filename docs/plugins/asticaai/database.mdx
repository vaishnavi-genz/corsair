---
title: Database
description: "Astica AI local sync: searchable entities, `.search()` filters, and operators."
---

The Astica AI plugin syncs data locally. Use `corsair.asticaai.db.<entity>.search({ data, limit?, offset? })` with the filters listed per entity.

<Info>
**New to Corsair?** See [database operations](/concepts/database), [data synchronization](/concepts/integrations), and [multi-tenancy](/concepts/multi-tenancy).
</Info>

## Audio Transcripts

Path: `asticaai.db.audioTranscripts.search`

```ts
const rows = await corsair.asticaai.db.audioTranscripts.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |
| `inputFingerprint` | `string` | equals, contains, startsWith, endsWith, in |
| `inputLength` | `number` | equals, gt, gte, lt, lte, in |
| `modelVersion` | `string` | equals, contains, startsWith, endsWith, in |
| `text` | `string` | equals, contains, startsWith, endsWith, in |
| `resultURI` | `string` | equals, contains, startsWith, endsWith, in |
| `transcribedAt` | `date` | equals, before, after, between |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

## Read Text Results

Path: `asticaai.db.readTextResults.search`

```ts
const rows = await corsair.asticaai.db.readTextResults.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |
| `inputFingerprint` | `string` | equals, contains, startsWith, endsWith, in |
| `inputLength` | `number` | equals, gt, gte, lt, lte, in |
| `modelVersion` | `string` | equals, contains, startsWith, endsWith, in |
| `content` | `string` | equals, contains, startsWith, endsWith, in |
| `pageCount` | `number` | equals, gt, gte, lt, lte, in |
| `lineCount` | `number` | equals, gt, gte, lt, lte, in |
| `readAt` | `date` | equals, before, after, between |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

