---
title: Webhooks
description: "Google Calendar incoming webhooks: event paths, payloads, and response data."
---

The Google Calendar plugin handles incoming webhooks. Point your provider’s subscription URL at your Corsair HTTP handler (see [Overview](/plugins/googlecalendar/overview) for setup context and the exact URL shape).

<Info>
**New to Corsair?** See [webhooks](/concepts/webhooks) and [hooks](/concepts/hooks).
</Info>

## Webhook map

- `onEventChanged` (`onEventChanged`)

## HTTP handler setup

```ts app/api/webhook/route.ts
import { processWebhook } from "corsair";
import { corsair } from "@/server/corsair";

export async function POST(request: Request) {
    const headers = Object.fromEntries(request.headers);
    const body = await request.json();
    const result = await processWebhook(corsair, headers, body);
    return result.response;
}
```

## Events

## On Event Changed

### On Event Changed

`onEventChanged`

A Google Calendar event was created, updated, or deleted

**Payload**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `message` | `object` | No | — |
| `subscription` | `string` | No | — |
| `event` | `any` | No | — |

<AccordionGroup>
<Accordion title="message full type">

```ts
{
  data?: string,
  attributes?: {
  },
  messageId?: string,
  publishTime?: string
}
```
</Accordion>
</AccordionGroup>



<AccordionGroup>
<Accordion title="Response data full type">

```ts
{
  type: eventCreated,
  calendarId: string,
  event: custom,
  timestamp: string
} | {
  type: eventUpdated,
  calendarId: string,
  event: custom,
  timestamp: string
} | {
  type: eventDeleted,
  calendarId: string,
  eventId: string,
  timestamp: string
}
```
</Accordion>
</AccordionGroup>

**`webhookHooks` example**

```ts
googlecalendar({
    webhookHooks: {
        onEventChanged: {
            before(ctx, args) {
                return { ctx, args };
            },
            after(ctx, response) {
            },
        },
    },
})
```

---

