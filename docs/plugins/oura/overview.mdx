---
title: Overview
description: "Wearable health platform API for sleep, readiness, activity, and wellness ring data."
---

Use **Oura** through Corsair: one client, typed API calls, local DB sync, and incoming webhooks.

**What you get:**

- 4 typed API operations
- 4 synced entities (`dailyActivity`, `dailyReadiness`, `dailySleep`, `personalInfo`) for fast `.search()` / `.list()`
- 3 incoming webhook event types

## Setup

<Steps>

<Step title="Install">
<CodeGroup>
```bash npm
npm install corsair @corsair-dev/oura
```
```bash yarn
yarn add corsair @corsair-dev/oura
```
```bash pnpm
pnpm install corsair @corsair-dev/oura
```
```bash bun
bun add corsair @corsair-dev/oura
```
</CodeGroup>

</Step>

<Step title="Add the plugin">
```ts corsair.ts
import Database from 'better-sqlite3';
import { createCorsair } from 'corsair';
import { oura } from '@corsair-dev/oura';

export const corsair = createCorsair({
	plugins: [
		oura(),
	],
	database: new Database('corsair.db'),
	kek: process.env.CORSAIR_KEK!,
	hub: {
		projectApiKey: process.env.CORSAIR_API_KEY!,
		signingSecret: process.env.CORSAIR_SIGNING_SECRET!,
	},
});
```

Multi-tenancy is the default — scope calls with `corsair.withTenant(id)`. See [Quick start](/quick-start) for KEK + Hub keys, and [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Step>

<Step title="Choose authentication">
<Tabs>
<Tab title="API Key (Recommended)">

No setup required yet. When you make your first request as a tenant, Corsair prompts for the API key.

Provider walkthrough: [Get Credentials](/plugins/oura/get-credentials).

```ts
oura()
```

More: [API Key](/concepts/api-key)

</Tab>
</Tabs>

</Step>

<Step title="Connect a tenant">
Mint a connect link and send the tenant to it. Hub hosts the page and delivers the result to your app — see [Connect / OAuth](/management/connect).

```ts
const { connectUrl } = await corsair.manage.connect.createLink({
	plugin: 'oura',
	tenantId: 'acme',
});
// redirect the user's browser to connectUrl
```

</Step>

</Steps>

## Example API calls

**`profile.get`**

```ts
const tenant = corsair.withTenant('acme');
await tenant.oura.api.profile.get({});
```


**Write**

_No write-style endpoint inferred; pick any operation from the [API](/plugins/oura/api) reference._


See the full list on the [API](/plugins/oura/api) page.

## Query synced data

Synced entities support `tenant.oura.db.<entity>.search()` and `.list()`: `dailyActivity`, `dailyReadiness`, `dailySleep`, `personalInfo`.

See [Database](/plugins/oura/database) for filters and operators.

## Webhooks

This plugin registers **3** webhook event types. Configure the provider to POST to your Corsair HTTP handler, then use `webhookHooks` on the plugin factory.

See [Webhooks](/plugins/oura/webhooks) for every event path and payload shape, and [Webhooks concept](/concepts/webhooks) for routing.

## What's next

<CardGroup cols={2}>
  <Card title="API reference" href="/plugins/oura/api">
    Every `oura.api.*` operation with input and output types.
  </Card>
  <Card title="Database" href="/plugins/oura/database">
    Synced entities, search filters, and operators.
  </Card>
  <Card title="Webhooks" href="/plugins/oura/webhooks">
    Event paths, payloads, and `webhookHooks` examples.
  </Card>
  <Card title="Connect / OAuth" href="/management/connect">
    createLink, Hub delivery, and tenant connect flows.
  </Card>
  <Card title="Use with agents" href="/mcp-adapters/mcp-adapters">
    Expose this plugin's operations as MCP tools.
  </Card>
  <Card title="Get credentials" href="/plugins/oura/get-credentials">
    Provider-console walkthrough for keys and OAuth apps.
  </Card>
</CardGroup>
