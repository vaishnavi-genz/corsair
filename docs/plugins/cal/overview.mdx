---
title: Overview
description: "Open scheduling platform for booking meetings, availability, and calendar integrations."
---

Use **Cal.com** through Corsair: one client, typed API calls, local DB sync, and incoming webhooks.

**What you get:**

- 7 typed API operations
- 1 synced entity (`bookings`) for fast `.search()` / `.list()`
- 5 incoming webhook event types

## Setup

<Steps>

<Step title="Install">
<CodeGroup>
```bash npm
npm install corsair @corsair-dev/cal
```
```bash yarn
yarn add corsair @corsair-dev/cal
```
```bash pnpm
pnpm install corsair @corsair-dev/cal
```
```bash bun
bun add corsair @corsair-dev/cal
```
</CodeGroup>

</Step>

<Step title="Add the plugin">
```ts corsair.ts
import Database from 'better-sqlite3';
import { createCorsair } from 'corsair';
import { cal } from '@corsair-dev/cal';

export const corsair = createCorsair({
	plugins: [
		cal(),
	],
	database: new Database('corsair.db'),
	kek: process.env.CORSAIR_KEK!,
	hub: {
		projectApiKey: process.env.CORSAIR_API_KEY!,
		signingSecret: process.env.CORSAIR_SIGNING_SECRET!,
	},
});
```

Multi-tenancy is the default — scope calls with `corsair.withTenant(id)`. See [Quick start](/quick-start) for KEK + Hub keys, and [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Step>

<Step title="Choose authentication">
<Tabs>
<Tab title="API Key (Recommended)">

No setup required yet. When you make your first request as a tenant, Corsair prompts for the API key.

Provider walkthrough: [Get Credentials](/plugins/cal/get-credentials).

```ts
cal()
```

More: [API Key](/concepts/api-key)

</Tab>
</Tabs>

</Step>

<Step title="Connect a tenant">
Mint a connect link and send the tenant to it. Hub hosts the page and delivers the result to your app — see [Connect / OAuth](/management/connect).

```ts
const { connectUrl } = await corsair.manage.connect.createLink({
	plugin: 'cal',
	tenantId: 'acme',
});
// redirect the user's browser to connectUrl
```

</Step>

</Steps>

## Example API calls

**`bookings.get`**

```ts
const tenant = corsair.withTenant('acme');
await tenant.cal.api.bookings.get({});
```


**`bookings.cancel`**

```ts
const tenant = corsair.withTenant('acme');
await tenant.cal.api.bookings.cancel({});
```


See the full list on the [API](/plugins/cal/api) page.

## Query synced data

Synced entities support `tenant.cal.db.<entity>.search()` and `.list()`: `bookings`.

See [Database](/plugins/cal/database) for filters and operators.

## Webhooks

This plugin registers **5** webhook event types. Configure the provider to POST to your Corsair HTTP handler, then use `webhookHooks` on the plugin factory.

See [Webhooks](/plugins/cal/webhooks) for every event path and payload shape, and [Webhooks concept](/concepts/webhooks) for routing.

## What's next

<CardGroup cols={2}>
  <Card title="API reference" href="/plugins/cal/api">
    Every `cal.api.*` operation with input and output types.
  </Card>
  <Card title="Database" href="/plugins/cal/database">
    Synced entities, search filters, and operators.
  </Card>
  <Card title="Webhooks" href="/plugins/cal/webhooks">
    Event paths, payloads, and `webhookHooks` examples.
  </Card>
  <Card title="Connect / OAuth" href="/management/connect">
    createLink, Hub delivery, and tenant connect flows.
  </Card>
  <Card title="Use with agents" href="/mcp-adapters/mcp-adapters">
    Expose this plugin's operations as MCP tools.
  </Card>
  <Card title="Get credentials" href="/plugins/cal/get-credentials">
    Provider-console walkthrough for keys and OAuth apps.
  </Card>
</CardGroup>
