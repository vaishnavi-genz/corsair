---
title: Overview
description: "No-code work management platform combining databases, automations, and team workflows."
---

Use **Workiom** through Corsair: one client, typed API calls, local DB sync.

**What you get:**

- 6 typed API operations
- 7 synced entities (`apps`, `fields`, `filters`, `lists`, `recordPages`, `records`, and 1 more) for fast `.search()` / `.list()`

## Setup

<Steps>

<Step title="Install">
<CodeGroup>
```bash npm
npm install corsair @corsair-dev/workiom
```
```bash yarn
yarn add corsair @corsair-dev/workiom
```
```bash pnpm
pnpm install corsair @corsair-dev/workiom
```
```bash bun
bun add corsair @corsair-dev/workiom
```
</CodeGroup>

</Step>

<Step title="Add the plugin">
```ts corsair.ts
import Database from 'better-sqlite3';
import { createCorsair } from 'corsair';
import { workiom } from '@corsair-dev/workiom';

export const corsair = createCorsair({
	plugins: [
		workiom(),
	],
	database: new Database('corsair.db'),
	kek: process.env.CORSAIR_KEK!,
	hub: {
		projectApiKey: process.env.CORSAIR_API_KEY!,
		signingSecret: process.env.CORSAIR_SIGNING_SECRET!,
	},
});
```

Multi-tenancy is the default — scope calls with `corsair.withTenant(id)`. See [Quick start](/quick-start) for KEK + Hub keys, and [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Step>

<Step title="Choose authentication">
<Tabs>
<Tab title="API Key (Recommended)">

No setup required yet. When you make your first request as a tenant, Corsair prompts for the API key.

```ts
workiom()
```

More: [API Key](/concepts/api-key)

</Tab>
</Tabs>

</Step>

<Step title="Connect a tenant">
Mint a connect link and send the tenant to it. Hub hosts the page and delivers the result to your app — see [Connect / OAuth](/management/connect).

```ts
const { connectUrl } = await corsair.manage.connect.createLink({
	plugin: 'workiom',
	tenantId: 'acme',
});
// redirect the user's browser to connectUrl
```

</Step>

</Steps>

## Example API calls

**`apps.getAll`**

```ts
const tenant = corsair.withTenant('acme');
await tenant.workiom.api.apps.getAll({});
```


**`records.create`**

```ts
const tenant = corsair.withTenant('acme');
await tenant.workiom.api.records.create({});
```


See the full list on the [API](/plugins/workiom/api) page.

## Query synced data

Synced entities support `tenant.workiom.db.<entity>.search()` and `.list()`: `apps`, `fields`, `filters`, `lists`, `recordPages`, `records`, `views`.

See [Database](/plugins/workiom/database) for filters and operators.

## What's next

<CardGroup cols={2}>
  <Card title="API reference" href="/plugins/workiom/api">
    Every `workiom.api.*` operation with input and output types.
  </Card>
  <Card title="Database" href="/plugins/workiom/database">
    Synced entities, search filters, and operators.
  </Card>
  <Card title="Connect / OAuth" href="/management/connect">
    createLink, Hub delivery, and tenant connect flows.
  </Card>
  <Card title="Use with agents" href="/mcp-adapters/mcp-adapters">
    Expose this plugin's operations as MCP tools.
  </Card>
</CardGroup>
