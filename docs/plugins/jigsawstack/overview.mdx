---
title: Overview
description: "AI utility API collection for OCR, translation, sentiment, and common ML microservices."
---

Use **JigsawStack** through Corsair: one client, typed API calls, local DB sync.

**What you get:**

- 23 typed API operations
- 3 synced entities (`prompts`, `summaries`, `transcriptions`) for fast `.search()` / `.list()`

## Setup

<Steps>

<Step title="Install">
<CodeGroup>
```bash npm
npm install corsair @corsair-dev/jigsawstack
```
```bash yarn
yarn add corsair @corsair-dev/jigsawstack
```
```bash pnpm
pnpm install corsair @corsair-dev/jigsawstack
```
```bash bun
bun add corsair @corsair-dev/jigsawstack
```
</CodeGroup>

</Step>

<Step title="Add the plugin">
```ts corsair.ts
import Database from 'better-sqlite3';
import { createCorsair } from 'corsair';
import { jigsawstack } from '@corsair-dev/jigsawstack';

export const corsair = createCorsair({
	plugins: [
		jigsawstack(),
	],
	database: new Database('corsair.db'),
	kek: process.env.CORSAIR_KEK!,
	hub: {
		projectApiKey: process.env.CORSAIR_API_KEY!,
		signingSecret: process.env.CORSAIR_SIGNING_SECRET!,
	},
});
```

Multi-tenancy is the default — scope calls with `corsair.withTenant(id)`. See [Quick start](/quick-start) for KEK + Hub keys, and [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Step>

<Step title="Choose authentication">
<Tabs>
<Tab title="API Key (Recommended)">

No setup required yet. When you make your first request as a tenant, Corsair prompts for the API key.

```ts
jigsawstack()
```

More: [API Key](/concepts/api-key)

</Tab>
</Tabs>

</Step>

<Step title="Connect a tenant">
Mint a connect link and send the tenant to it. Hub hosts the page and delivers the result to your app — see [Connect / OAuth](/management/connect).

```ts
const { connectUrl } = await corsair.manage.connect.createLink({
	plugin: 'jigsawstack',
	tenantId: 'acme',
});
// redirect the user's browser to connectUrl
```

</Step>

</Steps>

## Example API calls

**`ai.prediction`**

```ts
const tenant = corsair.withTenant('acme');
await tenant.jigsawstack.api.ai.prediction({});
```


**`ai.imageGeneration`**

```ts
const tenant = corsair.withTenant('acme');
await tenant.jigsawstack.api.ai.imageGeneration({});
```


See the full list on the [API](/plugins/jigsawstack/api) page.

## Query synced data

Synced entities support `tenant.jigsawstack.db.<entity>.search()` and `.list()`: `prompts`, `summaries`, `transcriptions`.

See [Database](/plugins/jigsawstack/database) for filters and operators.

## What's next

<CardGroup cols={2}>
  <Card title="API reference" href="/plugins/jigsawstack/api">
    Every `jigsawstack.api.*` operation with input and output types.
  </Card>
  <Card title="Database" href="/plugins/jigsawstack/database">
    Synced entities, search filters, and operators.
  </Card>
  <Card title="Connect / OAuth" href="/management/connect">
    createLink, Hub delivery, and tenant connect flows.
  </Card>
  <Card title="Use with agents" href="/mcp-adapters/mcp-adapters">
    Expose this plugin's operations as MCP tools.
  </Card>
</CardGroup>
