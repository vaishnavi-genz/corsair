---
title: Database
description: "You.com local sync: searchable entities, `.search()` filters, and operators."
---

The You.com plugin syncs data locally. Use `corsair.youcom.db.<entity>.search({ data, limit?, offset? })` with the filters listed per entity.

<Info>
**New to Corsair?** See [database operations](/concepts/database), [data synchronization](/concepts/integrations), and [multi-tenancy](/concepts/multi-tenancy).
</Info>

## Search Results

Path: `youcom.db.searchResults.search`

```ts
const rows = await corsair.youcom.db.searchResults.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |
| `url` | `string` | equals, contains, startsWith, endsWith, in |
| `title` | `string` | equals, contains, startsWith, endsWith, in |
| `description` | `string` | equals, contains, startsWith, endsWith, in |
| `thumbnail_url` | `string` | equals, contains, startsWith, endsWith, in |
| `page_age` | `string` | equals, contains, startsWith, endsWith, in |
| `favicon_url` | `string` | equals, contains, startsWith, endsWith, in |
| `query` | `string` | equals, contains, startsWith, endsWith, in |
| `searchedAt` | `date` | equals, before, after, between |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

