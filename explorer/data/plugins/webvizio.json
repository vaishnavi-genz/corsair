{
  "id": "webvizio",
  "displayName": "Webvizio",
  "description": "Website feedback tool for visual bug reports, annotations, and client review workflows.",
  "npmPackageName": "@corsair-dev/webvizio",
  "authTypes": ["api_key"],
  "counts": {
    "api": 2,
    "webhooks": 0,
    "db": 2
  },
  "auth": [
    {
      "authType": "api_key",
      "integrationFields": [],
      "accountFields": ["api_key", "webhook_signature", "one"]
    }
  ],
  "api": [
    {
      "path": "webvizio.api.projects.list",
      "shortPath": "projects.list",
      "description": "List all available Webvizio projects",
      "riskLevel": "read",
      "input": {
        "kind": "object",
        "fields": []
      },
      "output": {
        "kind": "inline",
        "type": "{ uuid: string, name: string, id?: number, externalId?: string | null, screenshot?: string | null, url?: string | null, createdAt?: string | null, updatedAt?: string | null }[]"
      }
    },
    {
      "path": "webvizio.api.webhooks.list",
      "shortPath": "webhooks.list",
      "description": "List all configured Webvizio webhook subscriptions",
      "riskLevel": "read",
      "input": {
        "kind": "object",
        "fields": []
      },
      "output": {
        "kind": "inline",
        "type": "{ id: number, url: string, event: string }[]"
      }
    }
  ],
  "webhooks": [],
  "db": [
    {
      "path": "webvizio.db.projects.search",
      "entityName": "projects",
      "filters": [
        {
          "field": "entity_id",
          "type": "string",
          "operators": ["equals", "contains", "startsWith", "endsWith", "in"]
        },
        {
          "field": "uuid",
          "type": "string",
          "operators": ["equals", "contains", "startsWith", "endsWith", "in"]
        },
        {
          "field": "name",
          "type": "string",
          "operators": ["equals", "contains", "startsWith", "endsWith", "in"]
        },
        {
          "field": "id",
          "type": "number",
          "operators": ["equals", "gt", "gte", "lt", "lte", "in"]
        },
        {
          "field": "externalId",
          "type": "string",
          "operators": ["equals", "contains", "startsWith", "endsWith", "in"]
        },
        {
          "field": "screenshot",
          "type": "string",
          "operators": ["equals", "contains", "startsWith", "endsWith", "in"]
        },
        {
          "field": "url",
          "type": "string",
          "operators": ["equals", "contains", "startsWith", "endsWith", "in"]
        },
        {
          "field": "createdAt",
          "type": "date",
          "operators": ["equals", "before", "after", "between"]
        },
        {
          "field": "updatedAt",
          "type": "date",
          "operators": ["equals", "before", "after", "between"]
        }
      ]
    },
    {
      "path": "webvizio.db.webhooks.search",
      "entityName": "webhooks",
      "filters": [
        {
          "field": "entity_id",
          "type": "string",
          "operators": ["equals", "contains", "startsWith", "endsWith", "in"]
        },
        {
          "field": "id",
          "type": "number",
          "operators": ["equals", "gt", "gte", "lt", "lte", "in"]
        },
        {
          "field": "url",
          "type": "string",
          "operators": ["equals", "contains", "startsWith", "endsWith", "in"]
        },
        {
          "field": "event",
          "type": "string",
          "operators": ["equals", "contains", "startsWith", "endsWith", "in"]
        }
      ]
    }
  ]
}
